<div class="blockwrapper">
{{#each blocks as |block|}}
    <div class="wrapper blockbox">
    {{#each block as |cell|}}
        <div class="box">{{cell}}</div>
    {{/each}}
    </div>
{{/each}}
</div>
